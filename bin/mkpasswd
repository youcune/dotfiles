#!/bin/sh

len=$1

[ -z $len ] && len="16"
char='ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnopqrstuvwxyz23456789-_.';
i=1
while [ $i -le $len ]; do
    dm=$(( (RANDOM % ${#char}) ))
    str="${str}${char:${dm}:1}"
    i=$(( i+1 ))
done

echo "$str"

